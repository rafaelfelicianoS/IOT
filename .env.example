# ============================================================================
# IoT Bluetooth Network - Configuration
# ============================================================================
# Copie este ficheiro para .env e ajuste os valores conforme necessário

# ----------------------------------------------------------------------------
# Bluetooth Configuration
# ----------------------------------------------------------------------------
# Interface Bluetooth a usar (hci0, hci1, etc.)
BLE_INTERFACE=hci0

# Tempo de scan em segundos
BLE_SCAN_TIMEOUT=10

# ----------------------------------------------------------------------------
# Network Configuration
# ----------------------------------------------------------------------------
# Intervalo de heartbeat em segundos (padrão: 5)
HEARTBEAT_INTERVAL=5

# Número máximo de heartbeats perdidos antes de considerar link down (padrão: 3)
HEARTBEAT_TIMEOUT=3

# TTL máximo para pacotes
MAX_TTL=10

# ----------------------------------------------------------------------------
# Security Configuration
# ----------------------------------------------------------------------------
# Curva elíptica para usar (padrão: secp521r1 = P-521)
ELLIPTIC_CURVE=secp521r1

# Diretório de certificados
CERTS_DIR=./certs

# Diretório de chaves privadas
KEYS_DIR=./keys

# CA certificate e key
CA_CERT_FILE=./certs/ca_cert.pem
CA_KEY_FILE=./keys/ca_key.pem

# Validade dos certificados em dias
CERT_VALIDITY_DAYS=365

# ----------------------------------------------------------------------------
# Logging Configuration
# ----------------------------------------------------------------------------
# Nível de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Diretório de logs
LOG_DIR=./logs

# ----------------------------------------------------------------------------
# Device Configuration
# ----------------------------------------------------------------------------
# Tipo de dispositivo (sink ou node)
DEVICE_TYPE=node

# NID do dispositivo (será gerado automaticamente se não especificado)
# DEVICE_NID=

# Path para o certificado do dispositivo
# DEVICE_CERT=

# Path para a chave privada do dispositivo
# DEVICE_KEY=

# ----------------------------------------------------------------------------
# Sink Configuration (apenas para o Sink)
# ----------------------------------------------------------------------------
# NID do Sink (para os nodes saberem qual é o Sink)
# SINK_NID=

# ----------------------------------------------------------------------------
# UI Configuration
# ----------------------------------------------------------------------------
# Refresh rate da UI em segundos
UI_REFRESH_RATE=1

# ----------------------------------------------------------------------------
# Development/Debug
# ----------------------------------------------------------------------------
# Ativar modo debug
DEBUG=false

# Desativar verificação de certificados (APENAS PARA TESTES!)
DISABLE_CERT_VERIFICATION=false
